# MongoDB Atlas connection string (replace <username>, <password>, and <cluster> values)
# Include the db name at the end, e.g., /StraySignal
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/StraySignal?retryWrites=true&w=majority

# Optional: if your URI doesn't include the db name already
# MONGODB_DB_NAME=StraySignal
